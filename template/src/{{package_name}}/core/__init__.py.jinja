"""Core package for {{ project_name }}."""
{% if database_type != 'none' %}
from {{ package_name }}.core.database import Base, engine, get_db, init_db

__all__ = ["Base", "engine", "get_db", "init_db"]
{% endif %}
