"""Tests for {{ package_name }}_common package."""

import pytest

from {{ package_name }}_common import __version__


def test_version() -> None:
    """Test that version is defined."""
    assert __version__ == "0.1.0"


@pytest.mark.unit
def test_sample(sample_fixture: str) -> None:
    """Sample test using fixture."""
    assert sample_fixture == "test_value"
